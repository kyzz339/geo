dependencies {
    implementation project(':geo-core')
    implementation project(':geo-common')
    implementation 'org.springframework.boot:spring-boot-starter-web'
}

bootJar {
    enabled = true
    mainClass = 'com.jae.geo.GeoApiApplication'
}
