package com.jae.geo.core.repository;

import com.jae.geo.core.entity.Store;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StoreRepository extends JpaRepository<Store, Long> {
}
